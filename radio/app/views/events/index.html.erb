<div id="events">
  <% @events.each do |event| %>
  <div class="event">
    <span class="date">
      <%= event.start_date.strftime("%b %d") %>
    </span>
    <% event.artists.collect do |artist| %>
      <%= link_to artist.name, artist, {:class=> "artist"} %>
    <% end %>
    <%= link_to event.venue.name, event.venue, {:class => "venue"}%>
  </div>
  <% end %>
</div>
