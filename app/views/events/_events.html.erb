
<% eventsDivId, videoDivId  = 'events', 'video' %>

<% content_for :sidebar do %>
  <div id="<%= videoDivId %>"></div>
  <a id="previous_song" href="#">Previous</a><a id="next_song" href="#">Next</a>
<% end %>

<div id="event_filters">
  <ul id="span_filters">
    <li><a id="today_filter" href="#">Today</a></li>
    <li><a id="week_filter" href="#">This Week</a></li>
    <li><a id="month_filter" href="#">This Month</a></li>
  </ul>
</div>

<table id="events">
  <% events.each do |event| %>
  <tr class="event">
    <td class="date"><%= event.start_date.strftime("%a %b %d") %></td>
    <td>
      <ul class="artists">
      <% event.artists.each do |artist| %>
        <li>
          <%= link_to artist.name, artist, {:class=> "artist"} %>
        </li>
      <% end %>
      </ul>
    </td>
  </tr>
  <tr class="venue_time_and_cost">
    <td>&nbsp;</td>
    <td>
      <%= link_to event.venue.name, event.venue, {:class => "venue"}%>
      <%= link_to event.start_date.strftime("%I:%M %p"), event, {:class => "time"}%>
      <span class="cost"><%= event.price_range_in_words %></span>
    </td>
  </tr>
  <% end %>
</table><!-- end #events -->

<script type="text/javascript">

  $j(document).ready( function() {
    var videoDivId = '<%= videoDivId %>';
    var playlistDivId = '<%= eventsDivId %>';
    var controller = new radio.Controller(videoDivId, playlistDivId);
  });

</script>
