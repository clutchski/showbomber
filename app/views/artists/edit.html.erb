<%= form_for(@artist, :url => {:action => :update}) do |f| %>
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@artist.errors.count, "error") %> prohibited this
      artist from being saved:
      </h2>

      <ul>
      <% @artist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.fields_for :songs do |sf| %>
    <div class="field">
      <%= sf.label :url %><br />
      <%= sf.text_field :url %>
    </div>
  <% end %>

  <input name="new_song">


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
